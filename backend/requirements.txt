# ============================================================
# AegisRAG — Dependencias Python (Fase 1)
# Pinneadas a versiones mayores para estabilidad.
# ============================================================

# --- Framework API ---
fastapi[standard]>=0.128.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# --- Configuración ---
pydantic-settings>=2.4.0,<3.0.0

# --- Base de datos relacional (async) ---
sqlalchemy[asyncio]>=2.0.30,<2.1.0
asyncpg>=0.30.0,<1.0.0

# --- Base de datos vectorial ---
qdrant-client>=1.12.0,<2.0.0

# --- Task queue ---
celery[redis]>=5.4.0,<6.0.0

# --- Redis (cliente async) ---
redis>=5.0.0,<6.0.0

# --- Procesamiento de documentos ---
langchain-text-splitters>=0.3.0,<1.0.0
pypdf>=5.0.0,<6.0.0
pypdfium2>=4.30.0,<5.0.0

# --- Embeddings locales (ONNX Runtime) ---
fastembed>=0.6.0,<1.0.0

# --- Driver sync PostgreSQL (para Celery workers) ---
psycopg2-binary>=2.9.0,<3.0.0

# --- HTTP Client (health checks internos) ---
httpx>=0.27.0,<1.0.0

# --- OCR rápido ONNX ---
rapidocr-onnxruntime>=1.2.0,<2.0.0

